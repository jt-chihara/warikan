# Database Configuration (Local Development)
POSTGRES_DB=warikan
POSTGRES_USER=warikan
POSTGRES_PASSWORD=warikan_dev_password
DATABASE_URL=postgres://warikan:warikan_dev_password@localhost:5432/warikan?sslmode=disable

# Database Configuration (Production - Supabase)
# DATABASE_URL=postgresql://postgres:[password]@[host]:[port]/[database]?sslmode=require

# Service Ports
DB_PORT=5432
PORT=8080  # Backend port
FRONTEND_PORT=3000

# Service URLs (Local Development)
VITE_API_MODE=rest
VITE_REST_ENDPOINT=http://localhost:8080
VITE_API_MODE=graphql # set to 'rest' to use Rust REST API
VITE_REST_ENDPOINT=http://localhost:8081

# Service URLs (Production)
# VITE_REST_ENDPOINT=https://warikan-api.your-domain.com
# FRONTEND_URLS=https://warikan.your-domain.com,https://www.warikan.your-domain.com
# ALLOWED_ORIGINS=https://additional-domain.com

# Development Settings
NODE_ENV=development
GO_ENV=development
