{
  "name": "warikan-gateway",
  "version": "1.0.0",
  "scripts": {
    "build": "go build -o bin/gateway ./cmd",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}